<atom
  id="@ios-swift/rule-prefer-Sendable"
  kind="rule"
  version="0.1.0"
  domain="ios-swift"
  tokens="316"
  q="4.0">
  <description>Under Swift 6 strict concurrency, any value passed across an `actor` boundary, into a detached task, or stored in `@Sendable` state must conform to `Sendable`. Public APIs benefit from being Sendable-by-default: it lets clients use the type freely from any isolation context. For value types with Sendable stored properties the conformance is automatic; for reference types it requires the type to be immutable, an `actor`, or to enforce its own synchronisation.</description>
  <projection>
    <level name="summary" file="chunks/summary.md" tokens="128"/>
    <level name="core"    file="chunks/core.md"    tokens="316"/>
    <level name="full"    file="chunks/full.md"    tokens="340"/>
  </projection>
</atom>
