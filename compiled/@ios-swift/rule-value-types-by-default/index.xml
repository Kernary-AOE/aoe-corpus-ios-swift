<atom
  id="@ios-swift/rule-value-types-by-default"
  kind="rule"
  version="0.1.0"
  domain="ios-swift"
  tokens="280"
  q="4.0">
  <description>Value types — `struct`, `enum`, tuples — give Swift its data-race safety, predictable equality, and copy-on-write performance. The standard library is built on them: `Array`, `Dictionary`, `String`, `Set`, `Date`, all `URL`-derived types. Use them as the default for any new type that represents data, configuration, or a record. Reach for `class` only when identity, deinit cleanup, or shared mutable state through references is genuinely required.</description>
  <projection>
    <level name="summary" file="chunks/summary.md" tokens="126"/>
    <level name="core"    file="chunks/core.md"    tokens="280"/>
    <level name="full"    file="chunks/full.md"    tokens="301"/>
  </projection>
</atom>
